# Client-local secrets and hardware overrides (update.sh preserves these)
.env.local
.env.client
.hardware-profile
*.bak

# Operator notes / duplicate downloads — not part of the public stack
STRATEGY-MONETIZATION.md
install (1).sh

# Uploaded / mounted working files
projects/*
!projects/.gitkeep
uploads/*
!uploads/.gitkeep

# Local leftovers — never commit keys
licenses.json
license.key
license/key

# OS / editor
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
*.swp
